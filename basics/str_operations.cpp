#include<iostream>

int main(int argc, char const *argv[])
{
    // .clear() clears the string value completly
    // .append() is used to  add anything after the string value
    // <str_name>.at(0) displays that index 0 of string .
    // <str_name>.insert(1 , '@') inserts the '@' at the index 1 of string and just pushes rest of it ahead 
    // <str_name>.find(' ') finds certain stuff which is present in the string , if present , returns its index
    // <str_name>.erase(0,3) erases the indexes from 0 to 3 of the string value.
    
    return 0;
}
